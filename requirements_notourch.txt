# AST 项目轻量级依赖 (不包含 ASR 和音频处理)
# 使用场景: 仅 LLM 聊天功能，无需语音转文本

# Web 框架
fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets==12.0

# HTTP 客户端
requests==2.31.0
httpx==0.25.2

# JSON 处理 (Python 3.7+ 自带，但为确保兼容性列出)
# json (built-in)

# 异步支持 (Python 3.7+ 自带)
# asyncio (built-in)

# 数据处理
python-multipart==0.0.6

# OpenAI 兼容客户端
openai==1.3.7

# 其他
pydantic==2.5.0
python-dateutil==2.8.2
soundfile


PyPDF2
